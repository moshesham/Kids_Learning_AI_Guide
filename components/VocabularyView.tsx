import React from 'react';

const VocabularyView: React.FC = () => {
    return <div>Vocabulary exercise view. This component is not currently used in the main application flow.</div>
};

export default VocabularyView;
