import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kids.learning.ai.guide',
  appName: 'Kids Learning AI Guide',
  webDir: 'dist'
};

export default config;
